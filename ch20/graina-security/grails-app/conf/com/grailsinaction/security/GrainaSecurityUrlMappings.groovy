package com.grailsinaction.security

class GrainaSecurityUrlMappings {
    static mappings = {
        /* These cannot be overridden by the application at present
        "/login"(controller: "login", action: "index")
        "/auth"(controller: "login", action: "signIn")
        "/logout"(controller: "login", action: "signOut")
        */
    }
}
